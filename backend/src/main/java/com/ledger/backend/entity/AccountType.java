package com.ledger.backend.entity;

/**
 * The type of ledger account chosen by the user at creation time.
 * Displayed as a dropdown on the frontend.
 */
public enum AccountType {
    SAVINGS,
    CURRENT,
    BUSINESS
}
